<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>edit todo list</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    />
    <style>
      button {
        border-radius: 1rem;
        padding: 0.5rem 1rem;
      }

      body {
        margin: 20px;
      }
    </style>
  </head>
  <body>
    <p>
      list id: <b><u><%= list._id %></u></b>
    </p>
    <form class="row g-3" method="POST" action="/lists/<%= list._id %>?_method=PUT">
      <div class="cols-auto">
        <textarea class="form-control" name="name"><%= list.name %></textarea
        ><br />
        <textarea class="form-control" name="time"><%= list.time %></textarea
        ><br />
        <button class="btn btn-primary">EDIT</button>
      </div>
    </form>
  </body>
</html>
